/**
 * dashboard-service.test.ts — Tests for dashboard service.
 *
 * Source module (src/domain/serve/dashboard-service.ts) was removed.
 * Tests are skipped until the module is restored or tests are deleted.
 */

import { describe, it, expect } from "vitest";

describe.skip("dashboard-service (source removed)", () => {
	it("placeholder", () => {
		expect(true).toBe(true);
	});
});
