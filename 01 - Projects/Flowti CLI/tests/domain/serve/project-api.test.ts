/**
 * project-api.test.ts — Tests for project API.
 *
 * Source module (src/domain/serve/project-api.ts) was removed.
 * Tests are skipped until the module is restored or tests are deleted.
 */

import { describe, it, expect } from "vitest";

describe.skip("project-api (source removed)", () => {
	it("placeholder", () => {
		expect(true).toBe(true);
	});
});
