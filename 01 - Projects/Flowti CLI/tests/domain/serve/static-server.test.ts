/**
 * static-server.test.ts — Tests for static server.
 *
 * Source module (src/domain/serve/static-server.ts) was removed.
 * Tests are skipped until the module is restored or tests are deleted.
 */

import { describe, it, expect } from "vitest";

describe.skip("static-server (source removed)", () => {
	it("placeholder", () => {
		expect(true).toBe(true);
	});
});
