<form method="post" action="{{ path('app_data_delete', {'id': data.id}) }}" onsubmit="return confirm('common.question.delete');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ data.id) }}">
    <button class="btn">{{ 'common.delete'|trans }}</button>
</form>
