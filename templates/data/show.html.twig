{% extends 'base.html.twig' %}

{% block title %}Data{% endblock %}

{% block body %}
    <h1>Data</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ data.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ data.title }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ data.type }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ data.category }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ data.status }}</td>
            </tr>
            <tr>
                <th>Body</th>
                <td>{{ data.body }}</td>
            </tr>
            <tr>
                <th>Relations</th>
                <td>{{ data.relations ? data.relations|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_data_index') }}">back to list</a>

    <a href="{{ path('app_data_edit', {'id': data.id}) }}">edit</a>

    {{ include('data/_delete_form.html.twig') }}
{% endblock %}
